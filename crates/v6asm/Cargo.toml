[package]
name = "v6asm"
version = "0.1.0"
edition = "2021"

[dependencies]
v6_core = { path = "../v6_core" }
clap = { workspace = true }
log = { workspace = true }
env_logger = { workspace = true }

[build-dependencies]
chrono = { version = "0.4", default-features = false, features = ["clock"] }
